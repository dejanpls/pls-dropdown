declare module 'pls-dropdown' {
  /**
   * Toggles the visibility of a dropdown list when a toggle element is clicked.
   *
   * @param toggleId The ID of the toggle element.
   * @param listId The ID of the dropdown list element.
   */
  export default function dropdown(toggleId: string, listId: string): void;
}
